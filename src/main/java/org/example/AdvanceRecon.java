package org.example;

public interface AdvanceRecon extends VisualRecon, SignalIntel {
    // This interface is a combination of two contracts.
    // It doesn't need a body, it just aggregates other interfaces.
}